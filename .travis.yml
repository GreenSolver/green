language: java

script:
 - ant;
 - ant test;


sudo: required

services:
  - docker
  
  
before_install:
- docker build -t floraJuliane/green .
- docker run -d -p 127.0.0.1:80:4567 floraJuliane/green /bin/sh -c "cd /root/green; ant;"
- docker ps -a
- docker run floraJuliane/green/bin/sh -c "cd /root/green; ant test"